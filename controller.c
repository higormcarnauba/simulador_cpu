#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

typedef struct {
    uint16_t R[8];
    uint16_t PC;
    uint16_t IR;
    uint16_t SP;
    uint8_t FLAGS[4];
} SIMULADOR;

uint8_t memory[]



// Conversão de hexadecimal para binário
// Identificar a instrução e executa-la
// Os prints, memoria
// Os registradores, as flags, pilha e memoria

